# Without changing `zero` and `one`
# and using only those variables,
# have the program print '01'

zero = 0 # do not change this line
one = '1' # do not change this line

# have the program print '01'
